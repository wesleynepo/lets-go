package main

import (
	"bytes"
	"io"
	"net/http"
	"net/http/httptest"
	"testing"

	"snippetbox.wesleynepo.com/internal/assert"
)

func TestPing(t *testing.T) {
    app := &application{

    }
    assert.Equal(t, rs.StatusCode, http.StatusOK)

    defer rs.Body.Close()

    body, err := io.ReadAll(rs.Body)
    if err != nil {
        t.Fatal(err)
    }

    bytes.TrimSpace(body)

    assert.Equal(t, string(body), "OK")
}
